smtp:
  # Gmail configuration (commented out, kept for reference)
  # server: "smtp.gmail.com"
  # port: 587
  # use_tls: true
  # username: "znaixian@gmail.com"
  # password_file: "config/email_password.txt"  # Gitignored

  # Outlook/Office 365 configuration (for local use)
  # server: "smtp.office365.com"
  # port: 587
  # use_tls: true
  # username: "naixian.carucci@factset.com"
  # password_file: "config/email_password.txt"

  # FactSet.io internal SMTP (no authentication required)
  server: "mail-internalonly.factset.com"
  port: 25
  use_tls: false
  username: "naixian.carucci@factset.com"
  password_file: ""  # No password needed for FactSet.io internal SMTP

recipients:
  success:
    - "naixian.carucci@factset.com"
  partial:
    - "naixian.carucci@factset.com"
  failure:
    - "naixian.carucci@factset.com"

attachments:
  include_logs: true
  include_summary_excel: false
  max_size_mb: 10
