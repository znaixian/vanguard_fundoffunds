# Streamlit Secrets Configuration Example
# =========================================
# This file shows the format for secrets needed in Streamlit Community Cloud
#
# HOW TO USE:
# 1. Go to https://share.streamlit.io/
# 2. Deploy your app from GitHub: znaixian/vanguard_fundoffunds
# 3. Click "Advanced settings" during deployment
# 4. Copy the content below (with your actual values) into the Secrets section
# 5. Click "Deploy"
#
# IMPORTANT: Never commit actual secrets to GitHub!
# This .example file should NOT contain real credentials

# ============================================================================
# REQUIRED SECRETS
# ============================================================================

# Anthropic Claude API Key
# Get your key from: https://console.anthropic.com
ANTHROPIC_API_KEY = "sk-ant-api03-your_actual_key_here"

# Project Root Path (use Streamlit's mount path)
PROJECT_ROOT = "/mount/src/vanguard_fundoffunds"

# Output and Logs Directories
OUTPUT_DIR = "output"
LOGS_DIR = "logs"

# ============================================================================
# CLAUDE AGENT CONFIGURATION (Optional - has defaults)
# ============================================================================

CLAUDE_MODEL = "claude-sonnet-4-5-20250929"
CLAUDE_MAX_TOKENS = "8000"

# ============================================================================
# FACTSET API CREDENTIALS (Required for calculations)
# ============================================================================

# FactSet API Username (from config/api_credentials.yaml)
FACTSET_USERNAME = "your_factset_username"

# FactSet API Key (from config/api-key.txt)
FACTSET_API_KEY = "your_factset_api_key"

# ============================================================================
# AWS S3 CONFIGURATION (Optional - only if using S3 uploads)
# ============================================================================

# AWS Access Key ID
AWS_ACCESS_KEY_ID = "your_aws_access_key_id"

# AWS Secret Access Key
AWS_SECRET_ACCESS_KEY = "your_aws_secret_access_key"

# AWS Region (should match your bucket's region)
AWS_DEFAULT_REGION = "us-east-1"

# S3 Bucket Name (from config/aws_config.yaml)
S3_BUCKET_NAME = "vanguard-fundoffunds-prod"

# Enable/Disable S3 (true/false)
S3_ENABLED = "true"

# ============================================================================
# EMAIL CONFIGURATION (Optional - only if using email notifications)
# ============================================================================

# Email settings (if using automated reports)
EMAIL_SENDER = "your_email@example.com"
EMAIL_PASSWORD = "your_email_app_password"
EMAIL_RECIPIENTS = "recipient1@example.com,recipient2@example.com"
SMTP_SERVER = "smtp.gmail.com"
SMTP_PORT = "587"

# ============================================================================
# DEVELOPMENT SETTINGS (Optional)
# ============================================================================

DEBUG_MODE = "false"
VERBOSE_LOGGING = "true"

# ============================================================================
# NOTES FOR STREAMLIT DEPLOYMENT
# ============================================================================
#
# 1. All values must be strings in TOML format (use quotes)
# 2. Booleans should be quoted strings: "true" or "false"
# 3. Numbers should be quoted strings: "8000" not 8000
# 4. For local development, create .streamlit/secrets.toml (gitignored)
# 5. For Streamlit Cloud, paste this content into the Secrets section
# 6. Update config loaders to read from st.secrets if deployed
#
# Example code to read secrets in your app:
#   import streamlit as st
#   if "ANTHROPIC_API_KEY" in st.secrets:
#       api_key = st.secrets["ANTHROPIC_API_KEY"]
#   else:
#       api_key = os.getenv("ANTHROPIC_API_KEY")
